---
description: Do not auto-deploy vercel-v2 to ima-summer-camp unless asked
alwaysApply: true
---

# Vercel v2 (IMA camp) — no automatic summer camp deploy

**Do not** run a production deploy to **`ima-summer-camp`** after changes under `vercel-v2` unless the user **explicitly** asks to deploy.

- Deploy from the **project root** (parent of `api/`), not from `api/` alone, only when requested. Command for summer camp when asked:

```bash
cd /Users/tomrichardson/Downloads/vercel-v2 && VERCEL_ORG_ID=team_PXMrLfQ01zlse3XBsDcaPa3s VERCEL_PROJECT_ID=prj_TJxRFRbKAK4AkYBe8AKL1DoH2k9p npx vercel deploy --prod --yes
```

- Otherwise do not run that deploy at the end of tasks.
